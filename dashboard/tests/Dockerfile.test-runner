# SOLARIA DFO - Test Runner Dockerfile
# Executes integration tests against test server

FROM node:22-alpine

WORKDIR /tests

# Install curl and jq for debugging
RUN apk add --no-cache curl jq

# Copy test files (will be overridden by volumes)
COPY tests/ ./

# Default command (can be overridden)
CMD ["node", "api.test.js"]
